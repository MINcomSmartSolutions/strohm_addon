<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="replace_bill_icon" name="Replace Bill Icon" inherit_id="account.portal_my_home_invoice">
        <xpath expr="//div[@id='portal_client_category']//t[@t-set='icon'][@t-value=&quot;'/account/static/src/img/Bill.svg'&quot;]" position="attributes">
            <attribute name="t-value">'/strohm_addon/static/src/img/receipt_long_64dp_434343_FILL0_wght400_GRAD0_opsz48.svg'</attribute>
        </xpath>
        <xpath expr="//div[@id='portal_vendor_category']//t[@t-set='icon'][@t-value=&quot;'/account/static/src/img/Bill.svg'&quot;]" position="attributes">
            <attribute name="t-value">'/strohm_addon/static/src/img/receipt_long_64dp_434343_FILL0_wght400_GRAD0_opsz48.svg'</attribute>
        </xpath>
    </template>
    
     <template id="replace_connection_icon" name="Replace Connection Icon" inherit_id="portal.portal_my_home">
        <xpath expr="//div[@id='portal_common_category']//t[@t-set='icon'][@t-value=&quot;'/portal/static/src/img/portal-connection.svg'&quot;]" position="attributes">
            <attribute name="t-value">'/strohm_addon/static/src/img/person_shield_64dp_434343_FILL0_wght400_GRAD0_opsz48.svg'</attribute>
        </xpath>
    </template>

    <!-- Set default title for all portal pages -->
    <!--TODO: Needs to be dynamic    -->
    <template id="custom_portal_layout_title" inherit_id="portal.portal_layout" name="Custom Portal Layout Title">
        <xpath expr="//t[@t-call='portal.frontend_layout']" position="inside">
            <t t-set="title" t-value="title or 'Portal - HM Laden'"/>
            <t t-set="x_icon">https://mediapool.hm.edu/media/_technik/img/_technik_1/favicon.jpg</t>
        </xpath>
    </template>

    <!-- Conditional container width adjustment - only applies when use_wide_container is set -->
    <template id="portal_container_width_conditional" name="Portal Container Width Conditional" inherit_id="portal.portal_layout">
        <xpath expr="//div[@id='wrap']/div[hasclass('container', 'pt-3', 'pb-5')]" position="attributes">
            <attribute name="t-attf-class">{{use_wide_container and 'container-fluid pt-3 pb-5 px-3 px-md-4' or 'container pt-3 pb-5'}}</attribute>
        </xpath>
    </template>

    <!-- Disable sale orders/quotations from portal home page using data record -->
    <record id="sale.portal_my_home_sale" model="ir.ui.view">
        <field name="active" eval="False"/>
    </record>

    <!-- Override portal_my_security template to remove password change section -->
    <template id="custom_sec" inherit_id="portal.portal_my_security" name="Custom Portal Security Fields" active='true'>
        <xpath expr="//t[@t-call='portal.portal_layout']" position="inside">
            <t t-set="title">HM Laden - Security</t>
        </xpath>
        <!-- Remove password change section -->
        <xpath expr="//section[@name='portal_change_password']" position="replace">
            <!-- This removes the password change section -->
        </xpath>

        <xpath expr="//section[@name='portal_revoke_all_devices_popup']" position="replace">
            <!-- This removes the revoke all devices popup no need for now-->
        </xpath>

        <!-- Modify account deactivation section -->
        <xpath expr="//section[@name='portal_deactivate_account']//form//hr" position="replace">
        <!-- Remove the horizontal line -->
        </xpath>

        <!-- Modify account deactivation section -->
        <xpath expr="//p[hasclass('text-muted')]//b" position="inside">
            <br/>You cannot delete your account if you have open invoices or active charging sessions.
            For information on data deletion policies, please refer to the <a href="https://backend.laden.hm.edu/faq">FAQ</a>.
        </xpath>

        <!-- Add error message display for deactivate account -->
        <xpath expr="//section[@name='portal_deactivate_account']//form" position="inside">
            <div t-if="error_message" class="alert alert-danger mt-2" role="alert">
                <t t-esc="error_message"/>
            </div>
        </xpath>

        <!-- Override the account deactivation section to remove password verification -->
        <xpath expr="//section[@name='portal_deactivate_account']//form//p[2]" position="replace">
            <!-- Remove the password verification text -->
        </xpath>

        <xpath expr="//section[@name='portal_deactivate_account']//input[@name='password']" position="replace">
            <!-- Remove the password input field -->
        </xpath>
        <xpath expr="//section[@name='portal_deactivate_account']//label[@for='request_blacklist']" position="replace">
            <!-- Remove the blacklist request label -->
        </xpath>
        <xpath expr="//section[@name='portal_deactivate_account']//input[@name='request_blacklist']" position="replace">
            <!-- Remove the blacklist request checkbox -->
        </xpath>

    </template>

    <!-- Completely disable TOTP portal section -->
    <template id="disable_totp_portal" inherit_id="auth_totp_portal.totp_portal_hook" name="Disable TOTP Portal" active='true'>
        <xpath expr="//div[@id='totp_wizard_view']" position="replace">
            <!-- Remove TOTP wizard view -->
        </xpath>
        <xpath expr="//section[h4/a[@href='https://www.odoo.com/documentation/master/applications/general/auth/2fa.html']]" position="replace">
            <!-- Remove entire TOTP section by targeting the documentation link which doesn't change with localization -->
        </xpath>
    </template>

    <!-- Override payment methods page title and favicon -->
    <template id="custom_payment_methods_title" inherit_id="payment.payment_methods" name="Custom Payment Methods Title">
        <xpath expr="//t[@t-call='portal.frontend_layout']" position="inside">
            <t t-set="title">HM Laden - Payment Methods</t>
            <t t-set="x_icon">https://mediapool.hm.edu/media/_technik/img/_technik_1/favicon.jpg</t>
        </xpath>
    </template>

    <template id="portal_active_charging_session_notice" inherit_id="portal.portal_my_home" name="Portal Active Charging Session Notice">
        <xpath expr="//div[hasclass('o_portal_my_home')]" position="before">
                <t t-if="has_active_charging_session">
                   <div id="charging_session_alert" class="alert alert-info" style="position: relative;">
                       <span class="blink-dot" style="position: absolute; top: 10px; right: 10px; display: inline-block; width: 12px; height: 12px; background-color: #0dcaf0; border-radius: 50%; animation: blink 1s infinite;"></span>
                       <p class="mb-2"><strong>You have an active charging session</strong></p>
                       <t t-if="active_charging_session_data and active_charging_session_data.get('start_timestamp')">
                           <p class="mb-0">Duration: <span id="charging_timer" t-att-data-start-time="active_charging_session_data['start_timestamp']">00:00:00</span></p>
                           <div id="charging_warning" class="mt-3" style="display: none;">
                               <p class="mb-0"><strong>⚠️ Notice:</strong> Your charging session has been running for over 10 hours. Please remember to remove your vehicle so that other users can also use the charging infrastructure.</p>
                           </div>
                           <script type="text/javascript">
                               (function() {
                                   var timerElement = document.getElementById('charging_timer');
                                   var warningElement = document.getElementById('charging_warning');
                                   var alertElement = document.getElementById('charging_session_alert');
                                   if (!timerElement) return;

                                   var startTime = new Date(timerElement.getAttribute('data-start-time'));
                                   var TEN_HOURS_MS = 10 * 60 * 60 * 1000;

                                   function updateTimer() {
                                       var now = new Date();
                                       var diff = now - startTime;

                                       var days = Math.floor(diff / (1000 * 60 * 60 * 24));
                                       var hours = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
                                       var minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
                                       var seconds = Math.floor((diff % (1000 * 60)) / 1000);

                                       var timeStr = '';
                                       if (days > 0) {
                                          if (days === 1) {
                                              timeStr += days + ' Day ';
                                          } else {
                                              timeStr += days + ' Days ';
                                          }
                                       }
                                       timeStr += String(hours).padStart(2, '0') + ':' +
                                                  String(minutes).padStart(2, '0') + ':' +
                                                  String(seconds).padStart(2, '0');

                                       timerElement.textContent = timeStr;

                                       // Show warning if more than 10 hours
                                       if (diff > TEN_HOURS_MS) {
                                           if (warningElement) {
                                               warningElement.style.display = 'block';
                                           }
                                           if (alertElement) {
                                               alertElement.classList.remove('alert-info');
                                               alertElement.classList.add('alert-warning');
                                           }
                                       }
                                   }

                                   updateTimer();
                                   setInterval(updateTimer, 1000);
                               })();
                           </script>
                           <style>
                               @keyframes blink {
                                   0%, 50% { opacity: 1; }
                                   51%, 100% { opacity: 0; }
                               }
                           </style>
                       </t>
                   </div>
                </t>
        </xpath>
    </template>

    <template id="portal_current_electricity_price" inherit_id="portal.portal_my_home" name="Portal Current Electricity Price">
        <xpath expr="//div[hasclass('o_portal_my_home')]" position="before">
        <t t-if="electricity_price_ct_kwh is not None">
            <div class="alert alert-info">
                    <span>Current Electricity Price is <b><t t-esc="electricity_price_ct_kwh"/> cent per kWh</b>
                        <t t-if="electricity_price_valid_till is not None">
                            and will be available till <b><t t-esc="electricity_price_valid_till"/></b>
                        </t>
                    </span>
                       <t t-if="electricity_price_valid_till is not None">
                             <t t-set="popover_content">Subject to change. Check back for the new cost of electricity after this prices' validation ends.</t>
                             <i tabindex="0" class="bi bi-info-square-fill d-inline-block" data-bs-toggle="popover" data-bs-trigger="hover focus" t-att-data-bs-content="popover_content"></i>
                       </t>
            </div>
        </t>
        </xpath>
    </template>

    <!-- Add external links to portal navbar -->
    <template id="custom_portal_navbar_links" inherit_id="web.frontend_layout" name="Custom Portal Navbar Links">
        <xpath expr="//ul[@id='top_menu']" position="inside">
            <li class="nav-item">
                <a href="https://backend.laden.hm.edu/faq" class="nav-link" target="_blank">FAQ</a>
            </li>
            <li class="nav-item">
                <a href="https://backend.laden.hm.edu/agb" class="nav-link" target="_blank">AGB</a>
            </li>
        </xpath>
    </template>

    <!-- Remove invoice_sending_method from portal details -->
    <template id="remove_invoice_sending_method" inherit_id="portal.portal_my_details_fields" name="Remove Invoice Sending Method">
        <xpath expr="//div[hasclass('row')][.//select[@name='invoice_sending_method']]" position="replace">
        </xpath>
    </template>

    <!-- Remove salesperson/purchase representative section from invoice page -->
    <template id="remove_salesperson_section" inherit_id="account.portal_invoice_page" name="Remove Salesperson Section">
        <xpath expr="//div[@t-if='invoice.invoice_user_id'][@class='flex-grow-1']" position="replace"/>
    </template>

    <!-- Remove "Your contact" section from sale order portal page -->
    <template id="remove_sale_order_contact_section" inherit_id="sale.sale_order_portal_template" name="Remove Sale Order Contact Section">
        <xpath expr="//div[@t-if='sale_order.user_id']" position="replace"/>
    </template>

    <!-- Dunning: Override payment.form to block payment for Mahnstufe 3 customers -->
    <template id="dunning_block_payment_form" inherit_id="payment.form" name="Dunning - Block Payment Form for M3">
        <!-- Step 1: Resolve whether this partner is in finance handover (Mahnstufe 3).
             `partner_id` is an integer in the payment form context. -->
        <xpath expr="//t[@t-set='mode']" position="before">
            <t t-set="finance_handover_blocked"
               t-value="partner_id and env['res.partner'].sudo().browse(partner_id).finance_handover or False"/>
        </xpath>

        <!-- Step 2: Add `and not finance_handover_blocked` to the form's t-if so it
             is hidden for blocked partners, triggering the t-else below. -->
        <xpath expr="//form[@id='o_payment_form']" position="attributes">
            <attribute name="t-if">(payment_methods_sudo or tokens_sudo) and not finance_handover_blocked</attribute>
        </xpath>

        <!-- Step 3: Replace the t-else block.
             - If blocked       -> show dunning notice.
             - Elif no methods  -> keep the original "no payment methods" warning.
             - Otherwise        -> nothing (the form above is visible).
             Using t-elif avoids the warning firing when finance_handover_blocked
             is simply False but payment methods are available. -->
        <xpath expr="//t[@t-call='payment.no_pms_available_warning']" position="replace">
            <t t-if="finance_handover_blocked">
                <div class="alert alert-danger" role="alert">
                    <strong>Zahlung über das Portal nicht möglich</strong>
                    <p class="mb-0 mt-2">
                        Aufgrund offener Forderungen wurde Ihr Konto an die Finanzverwaltung übergeben.
                        Eine Zahlung über das Nutzerportal ist daher nicht möglich.
                        Bitte wenden Sie sich zur Begleichung der offenen Beträge direkt an die Finanzverwaltung
                    </p>
                </div>
            </t>
            <t t-elif="not (payment_methods_sudo or tokens_sudo)" t-call="payment.no_pms_available_warning"/>
        </xpath>
    </template>
</odoo>